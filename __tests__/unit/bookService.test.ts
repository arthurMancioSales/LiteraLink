describe("Service deleteBook", () => {
    // it("should return 'Error: Usuário não encontrado!' if user not found", () => {

    // });
   
    // it("should return 'Error: Livro não encontrado!' if book not found", () => {

    // });
   
    // it("should delete book from user profile", () => {

    // });
});

describe("Service patchBook", () => {
    // it("should return 'Error: Usuário não encontrado!' if user not found", () => {

    // });
   
    // it("should return 'Error: Livro não encontrado!' if book not found", () => {

    // });
   
    // it("should update book from user profile", () => {

    // });
});

describe("Service postBook", () => {
    // it("should return 'Error: Usuário não encontrado!' if user not found", () => {

    // });
   
    // it("should insert book on user profile", () => {

    // });
});
